Place source file in Downloads folder, output will be generated at same location.

Run below command to execute:
py -m jupyter nbconvert --execute --to notebook --inplace LIST_CONVERTER.ipynb --ExecutePreprocessor.timeout=-1
